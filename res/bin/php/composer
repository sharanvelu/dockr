#!/bin/bash

## DockR by Sharan

shift 1

# Check if DockR is running
is_dockr_up

# Execute Composer command inside the container.
${DOCKER_COMPOSE_COMMAND} -f "${DOCKR_COMPOSE_FILE}" -p "${PROJECT_NAME}" exec web \
    composer $@
